rvm:
  - 2.0.0
gemfile:
  - 'Gemfile'
before_script:
  - "echo before_script"
before_install:
  - "echo before_install"
script: "RAILS_ENV=test ls -1 && echo DONE!"

cache:
  directories:
  - ~/.cache

image:
  - one
  - two
